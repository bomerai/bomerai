"""Django application configuration for the `configurable_variables` app."""

from django.apps import AppConfig


class ConfigurableVariablesConfig(AppConfig):
    """Django application configuration for the `configurable_variables` app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "configurable_variables"
    verbose_name = "Configurable Variables"
